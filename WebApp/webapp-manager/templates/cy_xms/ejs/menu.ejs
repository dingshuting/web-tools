<div class="sidebar-collapse">

	<ul class="nav" id="side-menu">
		<li class="nav-header">
			<div class="dropdown profile-element"> <span>
                             </span>
				<a data-toggle="dropdown" class="dropdown-toggle" href="index.html#" aria-expanded="false">
					<% if(!$.showdemo){%>
					<span class="clear"> <span class="block m-t-xs"> <strong class="font-bold"><%=$.session.currentUser.name==null?$.session.currentUser.accountNo:$.session.currentUser.name%></strong>
                             </span> <span class="text-muted text-xs block"><%if($.session.currentUser.roles&&$.session.currentUser.roles.length>0){%> <%=$.session.currentUser.roles[0].name%> <%}else{%>无角色<%}%><b class="caret"></b></span> </span>
					<% }%>
				</a>
				<!--<ul class="dropdown-menu animated fadeInRight m-t-xs">-->
				<!--	<li>
						<a href="##">修改头像</a>
					</li>-->
					<!--<li id="system.personal-details">-->
						<!--<a href="##">个人资料</a>-->
					<!--</li>-->
					<!--<li>
						<a href="##">联系我们</a>
					</li>
					<li>
						<a href="##">信箱</a>
					</li>-->
					<!--<li class="divider"></li>-->
					<!--<li id="logout">-->
						<!--<a href="##">安全退出</a>-->
					<!--</li>-->
				<!--</ul>-->
			</div>
			<div class="logo-element">
				H+
			</div>

		</li>
		<li id="m_index" class="index">
			<a href="index.html"><i class="fa iconfont icon-shouye2-copy"></i> <span class="nav-label">首页</span></a>
		</li>
		<% this.each(function(current) { %>
			<% if(current.attr("displayType")==1){ %>
		<li class="admin-parent" <%= (el) -> el.data('menu', current) %>>
			<a href="##" class='am-cf am-collapsed' >
				<i class="<%=current.attr("icon")%>"></i>
				<span class="nav-label"><%=current.attr("name") %></span>
				<% if(current.attr("children")&&current.attr("children").length>0){ %>
					<span class="fa arrow"></span>
				<%}%>
			</a>
				<% if(current.attr("children")&&current.attr("children").length>0){ %>
					<ul class="nav nav-second-level collapse" id="collapse-<%=current.attr("id")%>">
						<% current.attr("children").each(function(scurrent){%>
							<% if(scurrent.attr("displayType")==1){ %>

								<li style="display: <% if($.session.currentUser.owner=="100000"&&scurrent.attr("level")==0){ %>
                                                     none
								<%}%>" <%= (el) -> el.data('menu', scurrent) %>><a style="margin-left: 10px;"><span class="<%=scurrent.attr("icon")%>"></span><%=scurrent.attr("name")%></a></li>
							<%}%>
						<%})%>
					</ul>
				<%}%>
		</li>
		<%}%>
	<%})%>
	</ul>
</div>